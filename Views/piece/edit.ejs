<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Edit Painting</title>
</head>

<body>
    <nav>
        <a href="/auth/logout">Log Out</a>
    </nav>
    <h1>Edit Painting</h1>

    <form action="/users/<%= userId %>/pieces/<%= piece._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
        <div>
            <label for="title">Title</label>
            <input type="text" name="title" id="title" value="<%= piece.title %>">
        </div>
        <br>
        <div>
            <label for="description">Description</label>
            <input type="text" name="description" id="description" value="<%= piece.description %>">
        </div>
        <br>
        <div>
            <label for="price">Price</label>
            <input type="text" name="price" id="price" value="<%= piece.price %>">
        </div>
        <br>
        <div>
            <label for="image">Painting image:</label>
            <input type="file" name="image" id="image" value="<%= piece.image %>" accept="image/*" />
        </div>
        <br>
        <button type="submit">Save changes</button>
        <br>
        <br>
        <a href="/users/<%= userId %>">Back to main gallery</a>
    </form>
</body>

</html>